<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.a5a5lab.fapp.contacthomepages.ContactHomepagesDao">

	<resultMap id="resultMapObj" type="com.a5a5lab.fapp.contacthomepages.ContactHomepagesDto"></resultMap>
	  
	<select id="selectList" resultMap="resultMapObj">
		SELECT
			seq,
			main,
			url,
			contact_seq 
		FROM
		    contacthomepages
		WHERE 1=1		
		ORDER BY seq DESC	
	</select>	  
	  	  
</mapper>